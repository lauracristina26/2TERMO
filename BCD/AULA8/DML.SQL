-- Active: 1788267989003@@127.0.0.1@3306@smartcoffe_dml_lauracristina
-- SMARTCOFFE BASE DE DADOS - DML 
-- EXECUTE SEMPRE QUE QUISER RESTAURAR A AULA 

DROP DATABASE IF EXISTS SMARTCOFFE_DML_LAURACRISTINA; 
CREATE DATABASE SMARTCOFFE_DML_LAURACRISTINA;
USE SMARTCOFFE_DML_LAURACRISTINA;

-- TABELAS
CREATE TABLE CLIENTE (
    ID_CLIENTE INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    NOME VARCHAR(100) NOT NULL,
    EMAIL VARCHAR(120) UNIQUE,
    TELEFONE VARCHAR(15),
    CIDADE VARCHAR(60) NOT NULL,
    ATIVO BOOLEAN NOT NULL DEFAULT TRUE,
    DATA_CADASTRO DATETIME DEFAULT CURRENT_TIMESTAMP
);

DROP TABLE CLIENTE;
DROP DATABASE SMARTCOFFE_DML_LAURACRISTINA;

CREATE TABLE CATEGORIA (
    ID_CATEGORIA INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    NOME_CATEGORIA VARCHAR(60) NOT NULL UNIQUE
);

CREATE TABLE PRODUTO (
    ID_PRODUTO INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    NOME_PRODUTO VARCHAR(100) NOT NULL,
    PRECO DECIMAL(10,2) NOT NULL,
    ATIVO BOOLEAN NOT NULL DEFAULT TRUE,
    ID_CATEGORIA INT NOT NULL,
    -- REFERÊNCIA DE NOMENCLATURA DE FOREIGN KEY, SEMPRE ACRESENTAR ESSE TIPO DE DADO
    CONSTRAINT FK_PRODUTO_CATEGORIA FOREIGN KEY (ID_CATEGORIA) REFERENCES CATEGORIA(ID_CATEGORIA)
);

CREATE TABLE PEDIDO (
    ID_PEDIDO INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    DATA_PEDIDO DATETIME NOT NULL,
    STATUS_PEDIDO ENUM('ABERTO','PREPARANDO','FINALIZADO','CANCELADO') NOT NULL,
    VALOR_TOTAL DECIMAL(10,2) NOT NULL DEFAULT 0.00,
    ID_CLIENTE INT NOT NULL,
    CONSTRAINT FK_PEDIDO_CLIENTE FOREIGN KEY (ID_CLIENTE) REFERENCES CLIENTE(ID_CLIENTE)
);

CREATE TABLE ITEM_PEDIDO (
    ID_ITEM INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    ID_PEDIDO INT NOT NULL,
    ID_PRODUTO INT NOT NULL,
    QUANTIDADE INT NOT NULL,
    PRECO_UNITARIO DECIMAL(10,2) NOT NULL,
    OBSERVACAO VARCHAR(150),
    CONSTRAINT FK_ITEM_PEDIDO_PEDIDO FOREIGN KEY (ID_PEDIDO) REFERENCES PEDIDO(ID_PEDIDO),
    CONSTRAINT FK_ITEM_PRODUTO FOREIGN KEY (ID_PRODUTO) REFERENCES PRODUTO(ID_PRODUTO)
);

CREATE TABLE FORMA_PAGAMENTO (
    ID_FORMA_PAGAMENTO INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    DESCRICAO VARCHAR(40) NOT NULL UNIQUE
);

CREATE TABLE PAGAMENTO (
    ID_PAGAMENTO INT PRIMARY KEY AUTO_INCREMENT,
    ID_PEDIDO INT NOT NULL,
    ID_FORMA_PAGAMENTO INT NOT NULL,
    VALOR DECIMAL(10,2) NOT NULL,
    DATA_PAGAMENTO DATETIME,
    CONSTRAINT FK_PAGAMENTO_PEDIDO FOREIGN KEY (ID_PEDIDO) REFERENCES PEDIDO(ID_PEDIDO),
    CONSTRAINT FK_PAGAMENTO_FORMA_PAGAMENTO FOREIGN KEY (ID_FORMA_PAGAMENTO) REFERENCES FORMA_PAGAMENTO(ID_FORMA_PAGAMENTO)
);

-- INSERÇÃO DE DADOS
-- TODOS QUE PRECISAM RECEBER INFORMAÇÃO
INSERT INTO CLIENTE (NOME, EMAIL, TELEFONE, CIDADE, ATIVO, DATA_CADASTRO) VALUES 
('ANA SILVA','ANA@EMAIL.COM', '19999999001','LIMEIRA', TRUE, NULL),
('BRUNO SOUZA','BRUNO@EMAIL.COM', '19999999002','LIMEIRA', TRUE, NULL),
('CARLOS PEREIRA','CARLOS@EMAIL.COM', '19999999003','LIMEIRA', TRUE, NULL),
('DANIELA COSTA','DANIELA@EMAIL.COM', '19999999004','LIMEIRA', TRUE, NULL),
('ELIAS MORAES','ELIAS@EMAIL.COM', '19999999005','PIRACICABA', TRUE, NULL),
('FELIPE ALMEIDA','FELIPE@EMAIL.COM', '19999999006','LIMEIRA', TRUE, NULL),
('GABRIELA LIMA','GABRIELA@EMAIL.COM', '19999999007','AMERICANA', TRUE, NULL),
('HUGO MARTINS','HUGO@EMAIL.COM', '19999999008','RIO CLARO', TRUE, NULL);

SELECT * FROM CLIENTE

INSERT INTO CATEGORIA (NOME_CATEGORIA) VALUES
('CAFÉ'),
('BEBIDAS QUENTES'),
('BEBIDAS GELADAS'),
('SOBREMESAS'),
('LANCHES'),
('SANDUÍCHES'),
('SALGADOS'),
('DOCES');

SELECT * FROM CATEGORIA

INSERT INTO PRODUTO (NOME_PRODUTO, PRECO, ATIVO, ID_CATEGORIA) VALUES
('EXPRESSO', 5.00, TRUE, 1),
('CAPPUCCINO', 7.50, TRUE, 2),
('LATTE', 6.50, TRUE, 2),
('MOCHA', 8.00, TRUE, 2),
('CHOCOLATE QUENTE', 4.50, TRUE, 2),
('CHÁ VERDE', 4.00, TRUE, 2),
('REFRIGERANTE', 4.00, TRUE, 3),
('SUCO NATURAL', 5.50, TRUE, 3),
('ÁGUA MINERAL', 2.50, TRUE, 3),
('TORTA DE LIMÃO', 6.00, TRUE, 4),
('BOLO DE CHOCOLATE', 5.50, TRUE, 4);

SELECT * FROM PRODUTO

INSERT INTO PEDIDO (DATA_PEDIDO, STATUS_PEDIDO, VALOR_TOTAL, ID_CLIENTE) VALUES
('2024-06-01 10:00:00', 'ABERTO', 0.00, 1),
('2024-06-01 11:30:00', 'PREPARANDO', 0.00, 2),
('2024-06-01 12:15:00', 'FINALIZADO', 0.00, 3),
('2024-06-01 13:45:00', 'CANCELADO', 0.00, 4),
('2024-06-02 09:20:00', 'ABERTO', 0.00, 5),
('2024-06-02 10:50:00', 'PREPARANDO', 0.00, 6),
('2024-06-02 11:30:00', 'FINALIZADO', 0.00, 7),
('2024-06-02 12:45:00', 'CANCELADO', 0.00, 8),
(NOW(), 'ABERTO', 0.00, 8);

SET @PEDIDO = LAST_INSERT_ID();
SELECT @PEDIDO;

SELECT * FROM PEDIDO

INSERT INTO ITEM_PEDIDO (ID_PEDIDO, ID_PRODUTO, QUANTIDADE, PRECO_UNITARIO, OBSERVACAO) VALUES 
(1, 1, 2, 5.00, 'Sem açúcar'),
(1, 10, 1, 6.00, NULL),
(2, 2, 1, 7.50, 'Com canela'),
(2, 11, 2, 5.50, NULL),
(3, 3, 2, 6.50, NULL),
(2, 2, 1, 7.50, 'Bem passado'),
(4, 4, 2, 8.00, NULL);

SELECT * FROM ITEM_PEDIDO

INSERT INTO FORMA_PAGAMENTO (DESCRICAO) VALUES
('DINHEIRO'),
('CARTÃO DE CRÉDITO'),
('CARTÃO DE DÉBITO'),
('PIX');

SELECT * FROM FORMA_PAGAMENTO

INSERT INTO PAGAMENTO (ID_PEDIDO, ID_FORMA_PAGAMENTO, VALOR, DATA_PAGAMENTO) VALUES
(1, 2, 16.00, '2026-09-01 12:30;00'),
(4, 3, 35.50, '2026-09-02 11:00;00'),
(7, 1, 28.00, '2026-09-02 12:00;00'),
(8, 2, 14.00, '2026-09-02 13:00;00');

SELECT * FROM PAGAMENTO

-- EX 1: UPDATE OU ATUALIZAÇÃO DE DADOS
UPDATE cliente
SET TELEFONE = '19999990210'
WHERE ID_CLIENTE = 3; 
-- DETERMINAR O COMANDO WHERE SEMPRE !!! ;)

-- EX 2: ATUALIZANDO VÁRIOS CAMPOS
UPDATE cliente
SET TELEFONE = '199999981213',
    CIDADE = 'PIRACICABA'
WHERE ID_CLIENTE = 4;

-- EX 3: ATUALIZANDO VÁRIOS REGISTROS
UPDATE produto
SET PRECO = PRECO * 1.05
WHERE ID_CATEGORIA = 1;

-- EX 4: ATUALIZANDO SEM WHERE
-- UPDATE PRODUTO
-- SET PRECO = 1.00;
-- CUIDADO AO UTILIZAR ESSA LINHA !!!! DEIXA TODOS OS REGISTROS COM O MESMO VALOR

-- EX 5: ATUALIZANDO CONDICIONAIS
UPDATE produto
SET PRECO = CASE
    WHEN PRECO < 10 THEN PRECO * 1.10
    ELSE PRECO * 1.05
END
WHERE ativo = TRUE; 

-- EX 6: ATUALIZANDO PARA NULL 
UPDATE cliente
SET TELEFONE = NULL
WHERE ID_CLIENTE = 5;
-- TUDO ATUALIZAÇÕES OU MODIFICAÇÃO DE DADOS

-- EXCLUIR REGISTROS OU DADOS 
DELETE FROM cliente
WHERE ID_CLIENTE = 8;


-- EX 2: EXCLUIR REGISTRO COM ESTRUTURA LÓGICA
UPDATE PRODUTO
SET ATIVO = FALSE
WHERE ID_PRODUTO = 7;

UPDATE cliente
SET ATIVO = FALSE 
WHERE ID_CLIENTE = 6;


-- LIMPAR TABELA OU REMOVER TODOS OS REGISTROS 
TRUNCATE TABLE CLIENTE; 



-- CONSULTAR INFORMAÇÕES NO BCD
SELECT * FROM CLIENTE;
-- CONSULTA POR ID 
SELECT * FROM PRODUTO
WHERE ID_PRODUTO = 1;


-- MENSAGEM DE SUCESSO
SELECT 'BANCO DE DADOS SMARTCOFFE_DML_LAURACRISTINA CRIADO COM SUCESSO' AS mensagem;