const entrada = require("readline-sync");

const nomeArmazenados = [];

for (let i = 5; i = nomeArmazenados; i++) {
    let nome = entrada.question(`Digite o nome ${nome}`);
nomeArmazenados.push(nome);
    console.log(`${nome.length}- Nome`);
}
